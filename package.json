{
  "name": "dich-web",
  "private": true,
  "scripts": {
    "dev": "wrangler dev",
    "test": "vitest",
    "build": "tsc --noEmit",
    "deploy": "wrangler deploy"
  },
  "dependencies": {
    "@docshift/core": "^0.1.0",
    "hono": "^4.0.0"
  },
  "devDependencies": {
    "typescript": "^5.9.3",
    "vitest": "^2.0.0",
    "wrangler": "^3.0.0"
  }
}
