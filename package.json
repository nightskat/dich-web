{
  "name": "dich-web",
  "private": true,
  "scripts": {
    "dev": "wrangler dev",
    "test": "vitest",
    "build": "tsc --noEmit",
    "deploy": "wrangler deploy"
  },
  "dependencies": {
    "hono": "^4.0.0",
    "@docshift/core": "^0.1.0"
  },
  "devDependencies": {
    "wrangler": "^3.0.0",
    "typescript": "^5.6.0",
    "vitest": "^2.0.0"
  }
}
